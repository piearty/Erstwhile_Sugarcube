:: Config [script]
Config.saves.autosave = true;
Config.saves.isAllowed = function () {
    if (passage() === 'Start' || tags().includes('confession')) {
        return false;
    }
    return true; 
};